import { createStyles } from "@mantine/core";

export const style = createStyles((theme) => {
  return {
    bg: {
      position: "fixed",
      top: 0,
      left: 0,
      width: "100vw",
      height: "100vh",
    },
  }
});
